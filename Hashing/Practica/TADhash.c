#include "TADhash.h"
#include <stdio.h>
#include <stdlib.h>

void Inserat(elemento e)
{
	
	return;
	
};

elemento Buscar(char *parametro)
{
	elemento e;
	return e;
	
};

void Borrar(elemento e)
{
	
	
	
};

int Hash(char *parametro)
{
	int codigo,suma,i;
	codigo=0;
	return codigo;
	
};

void Leer(char *ruta)
{
	printf("Recibido");
	FILE *archivo;
	char *cadena;
	archivo=fopen(ruta,"r");
	int i;
	printf("\n1\n");
	for(i=0;i<20;i++)
	{
		fscanf(archivo,"\n%s",cadena);	
		printf("%s\n",cadena);
	}
	fclose(archivo);
	
}